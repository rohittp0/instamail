.idea/
.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/

# OSINT input emails and tool output (never commit harvested data)
emails.txt
emails.cleaned.txt
emails.dropped.tsv
out.csv
errors.csv
email_to_instagram.csv
email_to_instagram.checkpoint.json

# On-disk resolver/harvester cache
.cache/

# Local environment / secrets (loaded by the CLI at startup)
.env
